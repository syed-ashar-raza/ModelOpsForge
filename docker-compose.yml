services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      MODEL_NAME: ModelOpsForgeClassifier
      MODEL_ALIAS: champion
      MLFLOW_TRACKING_URI: sqlite:////app/mlflow.db
